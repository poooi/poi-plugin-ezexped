/*
   "p-state" is short for "persistent state",
   namely all info necessary to keep this plugin working.

   this was once called "config" but I figure it has gone beyond
   just config thus deserves a more suitable name.

 */

export * from './common'
export * from './load'
export * from './save'
